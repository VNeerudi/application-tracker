# Copy this file to `backend/.env` and fill in your own values.
# IMPORTANT: never commit `backend/.env` (it contains secrets).

# Email Configuration
EMAIL_PROVIDER=gmail
EMAIL_ADDRESS=your-email@gmail.com
EMAIL_APP_PASSWORD=your-email-app-password

# For Outlook, use:
# EMAIL_PROVIDER=outlook
# EMAIL_ADDRESS=your-email@outlook.com
# EMAIL_PASSWORD=your-password-or-app-password

# IMAP Settings (optional, defaults provided)
# IMAP_SERVER=imap.gmail.com
# IMAP_PORT=993

# Ollama Configuration
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama2

# Gemini Configuration (for portfolio extraction)
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-pro

# Database (optional)
# DATABASE_URL=sqlite:///./job_applications.db

